fastapi>=0.115,<1
uvicorn[standard]>=0.34,<1
pydantic>=2.10,<3
pandas>=2.0,<3
pyfao56==1.4.3
pcse==6.0.13
aquacrop==3.1.0  # runtime package pinned; engine_version is verified at execution
rasterio>=1.4,<2  # server-side GeoTIFF decode for AgriFM input contract
plantcv==4.11.3  # quantitative photo phenotyping evidence; no diagnostic authority
onnxruntime>=1.19,<2  # CPU-only independent PlantVillage shadow classifier
Pillow>=10.4,<13  # deterministic RGB resize/crop preprocessing for ONNX vision
cropforge==1.0.1  # real CropForge core shadow runtime; no production authority
